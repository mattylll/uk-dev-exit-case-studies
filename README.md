# UK Development Exit Finance Case Studies

Static build of `uk-dev-exit-case-studies` from the ConstructionCapCloudSites monorepo.

Deployable as a static site on Render, DigitalOcean App Platform, Fly.io, Azure Static Web Apps, or any root-serving static host — connect this repo, set publish directory to `/`, no build command needed.
